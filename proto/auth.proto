syntax = "proto3";

package auth;

option go_package = "pz1/proto/auth;authpb";

// Сервис проверки токена
service AuthService {
  rpc Verify(VerifyRequest) returns (VerifyResponse);
}

message VerifyRequest {
  string token = 1;
}

message VerifyResponse {
  bool valid = 1;
  string subject = 2;
}